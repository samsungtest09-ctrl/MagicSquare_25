# Magic Square 4×4 — AC-FR-01-01 RED 테스트·Boundary 스켈레톤 보고서

## 문서 정보

| 항목 | 내용 |
|---|---|
| **프로젝트** | Magic_Square_XX |
| **범위** | AC-FR-01-01 RED 테스트, `src/` 레이아웃, Boundary/Control 스켈레톤, 커버리지·결함 목록 |
| **기준 문서** | `docs/test_plan.md` (TP-MS-4X4-FR01), `docs/PRD_MagicSquare.md`, README RED To-Do |
| **작성일** | 2026-05-29 |
| **구현 상태** | Boundary P0 스켈레톤 GREEN(형상/null); Control `resolve()` 미구현; pytest **57 passed / 1 failed** |

---

## 1. 목적

- **AC-FR-01-01**(`grid=None` → `INVALID_SIZE`, `message="Grid must be 4x4."`)을 RED 테스트 앵커로 고정한다.
- Dual-Track TDD **Track A(Boundary)** / **Track B(격리)** 테스트를 작성하고, ECB 경계(`boundary` → `control`)를 검증한다.
- `src/` 레이아웃·`pytest.ini`·스켈레톤으로 테스트 실행 가능 상태를 만든 뒤, 커버리지·결함을 QA 산출물로 기록한다.

---

## 2. 수행 범위

### 2.1 문서·체크리스트

| 산출물 | 내용 |
|---|---|
| `README.md` | 「RED 단계 To-Do 리스트」 섹션 삽입 (Track A/B, 커버리지, 결함 연결) |
| `defect_list.md` | DEF-001~004 기록·검증 (Open: DEF-003) |

### 2.2 테스트 (RED → 부분 GREEN)

| 경로 | 역할 | 테스트 수 |
|---|---|---|
| `tests/conftest.py` | `INVALID_SIZE` 상수, grid fixture, out-of-scope 샘플 | — |
| `tests/boundary/test_boundary_validator_ac_fr_01_01.py` | Track A — 형상/null, 메시지·코드, DTO, 범위 메타 | 31 |
| `tests/control/test_solver_boundary_guard_ac_fr_01_01.py` | Track B — `resolve()` 0회, facade 격리 | 22 (21 통과) |

- Given–When–Then 주석, `# AC-FR-01-01`, `test_[조건]_[동작]_[검증]` 명명 규칙 적용.

### 2.3 구현 (스켈레톤)

| 경로 | 역할 |
|---|---|
| `src/boundary/constants.py` | `GRID_SIZE`, `INVALID_SIZE_*` |
| `src/boundary/models/validation_failure_result.py` | pydantic 실패 DTO |
| `src/boundary/validators/boundary_validator.py` | null·4×4 형상 검증 |
| `src/boundary/facade.py` | `validate_and_solve()` — 실패 시 `resolve()` 미호출 |
| `src/control/use_cases/solver.py` | `Solver.resolve()` — `NotImplementedError` |
| `pytest.ini` | `pythonpath = src` |

### 2.4 QA·측정

| 항목 | 결과 |
|---|---|
| `pytest tests/` | 57 passed, 1 failed |
| 커버리지 (boundary+control+entity) | **96%** (67 stmts, 3 miss) |
| HTML 리포트 | `htmlcov/index.html` |
| `defect_list.md` | 기존 문서 검증 — 현재 실패와 일치 |

### 2.5 미수행·의도적 제외

- AC-FR-01-02~05 (빈칸 개수, 범위, 중복) 테스트·구현
- FR-02~05 Solver 성공 경로·유효 4×4 처리
- DEF-003 테스트 설계 수정
- README RED To-Do 개별 TC 체크박스 일괄 `[x]` 처리 (회귀 GREEN 미완)

---

## 3. 테스트 실행 요약

```bash
python -m pytest tests/ -q
# 57 passed, 1 failed in ~0.2s
```

| Track | 통과 | 비고 |
|---|---|---|
| Track A — Boundary | 31/31 | AC-FR-01-01 P0 전항목 |
| Track B — Control | 20/21 | DEF-003 1건 실패 |
| Entity | 4/4 | 기존 `user` 테스트 |

**실패 테스트:** `test_isolation_module_does_not_patch_blank_finder`  
- `inspect.getsource(mod)` 전체에서 `"BlankFinder"` 금지 → assert·주석에 동일 문자열 포함 (테스트 자기 모순).

---

## 4. 커버리지 요약

| 레이어 | 목표 (README) | 측정값 | 미커버 라인 |
|---|---|---|---|
| Boundary | 85%+ | **~95%** | `facade.py:17`, `boundary_validator.py:25` |
| Control (Domain 진입) | 95%+ (통합 시) | **~88%** | `solver.py:11` |
| 전체 | 90%+ | **96%** | 유효 grid·`resolve()` 경로 |

미커버 3라인은 **유효 4×4 통과 후 `Solver.resolve()` 호출** 및 skeleton `NotImplementedError` — AC-FR-01-01 범위 외.

---

## 5. 결함 목록 연동 (`defect_list.md`)

| ID | Severity | 상태 | 요약 |
|---|---|---|---|
| DEF-001 | Critical | **Resolved** | `ModuleNotFoundError: boundary` → `src/` + `pytest.ini` |
| DEF-002 | Critical | **Resolved** | `grid=None` → `INVALID_SIZE` DTO |
| DEF-003 | Low | **Open** | 메타 테스트 `BlankFinder` 자기 참조 |
| DEF-004 | Info | Open (의도) | 유효 4×4 `NotImplementedError` |

README 결함 연결:
- `[x]` defect_list.md 생성 및 기록
- `[ ]` 모든 결함 수정 후 회귀 GREEN (DEF-003 해결 필요)

---

## 6. ECB·아키텍처 정합

```
[tests/boundary]  →  src/boundary/validators  (FR-01 형상)
[tests/control]   →  src/boundary/facade      (격리: Solver.resolve patch)
                      ↓ (성공 시만)
                    src/control/use_cases/solver
```

- **허용:** `boundary` → `control` (`facade`가 `Solver` 호출)
- **검증:** Boundary 실패 시 `resolve()` **0회** (mock 14건 + parametrized)
- **금지 준수:** `boundary` → `entity` 직접 의존 없음

---

## 7. 추적성 (AC-FR-01-01)

| README To-Do | 테스트·구현 | 상태 |
|---|---|---|
| TC-A-01~07 | `TestNormalFailureReturn` ~ `TestFailureResultStructure` | 구현·테스트 **통과** |
| TC-B-01~03 | `TestIsolationGuard`, `TestBoundaryHandlesNoneBeforeResolve`, `TestResolveMockGuard` | **통과** |
| TC-B-04 | `TestScopeExcludesDownstreamAcs` | 4/5 통과 (DEF-003) |
| 커버리지 85%/90% | pytest-cov | **달성** (Control 단독 95%는 미달) |

---

## 8. 핵심 설계 결정

| 결정 | 근거 |
|---|---|
| 공개 계약 `INVALID_SIZE` + `"Grid must be 4x4."` | README RED To-Do·사용자 AC 명시 (PRD `ERR-BND-*`와 병기) |
| 실패 반환 = `ValidationFailureResult` (예외 아님) | Track A 「Happy Path of Failure」 |
| `src/` 레이아웃 + `pythonpath=src` | `test_plan.md` §7.2 cov 타깃 정합 |
| skeleton에서 유효 grid `NotImplementedError` | P0 범위 = null·형상만; FR-02+ 별도 TP |

---

## 9. 다음 단계

| 순서 | 작업 | 완료 조건 |
|---|---|---|
| 1 | DEF-003 수정 | `pytest` 58/58 GREEN |
| 2 | README 결함 체크박스 | 「회귀 테스트 통과」 `[x]` |
| 3 | README RED To-Do | TC-A/B 항목 `[x]` (선택) |
| 4 | FR-01 P1 (AC-FR-01-02~05) | ERR-BND-003~005 RED 테스트 |
| 5 | `Solver.resolve()` GREEN | Domain 95%+ 커버리지 게이트 |

---

## 10. 결론

- AC-FR-01-01에 대한 **RED 테스트 53건**과 **Boundary/Control 스켈레톤**이 `src/` 구조로 정리되었다.
- 제품 로직(P0 null·형상)은 **31+20건 통과**; 잔여 1실패는 **테스트 설계 결함(DEF-003)** 이다.
- 커버리지 **96%**, Boundary 목표 **85% 초과** 달성; HTML 리포트 `htmlcov/` 생성 완료.
- 다음 QA 마일스톤: **DEF-003 해결 → 전체 GREEN → README 회귀 체크**.

---

*작성: AC-FR-01-01 RED·스켈레톤·커버리지·결함 QA 세션*
